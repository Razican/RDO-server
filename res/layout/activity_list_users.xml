<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="@dimen/Margin15"
	android:paddingRight="@dimen/Margin15"
	android:paddingBottom="@dimen/Margin15"
	android:paddingTop="@dimen/Margin15"
	android:layout_gravity="center"
	android:gravity="center"
    tools:context=".ListUsersActivity" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content" >

        <TextView
            android:id="@+id/textViewTitle"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:textSize="25sp"
            android:text="Lista de usuarios" />

        <ImageButton
            android:id="@+id/button_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="0"
            android:background="#00000000"
            android:src="@drawable/add_icon"
            android:contentDescription="Button to add user"/>
    </LinearLayout>

    <ListView
            android:id="@+id/list_Users"
            android:layout_width="fill_parent"
            android:layout_height="0dip"
            android:layout_weight="1"
            android:layout_marginBottom="15dp" >
        </ListView>

    <TextView
        android:id="@+id/text_view_users_connected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
		android:textSize="20sp"
        android:text="Usuarios conectados: 3" />

</LinearLayout>
